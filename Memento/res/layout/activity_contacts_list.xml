<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".ContactsListActivity" >

    <ListView
        android:id="@+id/listView_contacts"
        android:layout_width="fill_parent"
        android:layout_height="0dp"
        android:layout_weight="5" >

    </ListView>

    <Button
        android:id="@+id/button_add_contact"
        android:layout_width="fill_parent"
        android:layout_height="0dp"
        android:layout_gravity="bottom"
        android:layout_marginTop="10dp"
        android:layout_weight="1"
        android:drawableLeft="@drawable/socialaddperson"
        android:onClick="goToNewContactDetails"
        android:text="@string/contactsList_activity_button_add_contact" />

    <Button
        android:id="@+id/button_send_contacts"
        android:layout_width="fill_parent"
        android:layout_height="0dp"
        android:layout_gravity="bottom"
        android:layout_marginTop="5dp"
        android:layout_weight="1"
        android:drawableLeft="@drawable/socialforward"
        android:onClick="onSendContactsClicked"
        android:text="@string/contactsList_activity_button_send_contacts" />

</LinearLayout>